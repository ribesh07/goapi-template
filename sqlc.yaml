version: "2"

sql:
  - engine: "postgresql"
    schema: "sql/migrations"
    queries: "sql/queries"

    gen:
      go:
        package: "db"
        out: "internal/store"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true